[gd_scene load_steps=3 format=2]

[ext_resource path="res://Actor.tscn" type="PackedScene" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends \"res://Actor.gd\"

signal movement_input(dir_x, dir_y)

func _onready():
	is_player = true

func _input(event):
	if body and event.is_action_released(\"click\"):
		var target = body.get_global_mouse_position()
		emit_signal(\"movement_input\", target.x, target.y)
"

[node name="Player" type="Node2D"]
script = SubResource( 1 )

[node name="Actor" parent="." instance=ExtResource( 1 )]
